   /*
    //get the segment info of a given segmentID
    
    
    //get the street name of a given segment
    string street_name = singleton->get_hashTable_streetID_to_streetName(this_segment.streetID);
    
    //return diagonal of the segment
    t_bound_box text_bound(from_position, to_position);
    
    //return the dimension of the the segment according to its orientation
    text_bound = adjust_t_bound_boxW(text_bound);
    
    //XY coordinates of every curve point of a segment
    
    
    //number of curve points
    
    
    //middle curve point
    unsigned middle_point=curve_points/2;
    */
